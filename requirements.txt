# requirements.txt is now only used locally (pip install -r requirements.txt)
# Docker installs everything directly in the Dockerfile for layer caching control.
# Python 3.13+ needs wheels that exist only in newer releases.
numpy<2; python_version < "3.13"
numpy>=2.1.0; python_version >= "3.13"
fastapi==0.111.0
# Avoid pulling optional compiled extras (better cross-platform; especially on Windows).
uvicorn==0.29.0
python-multipart==0.0.9
python-dotenv==1.0.1
pillow==10.3.0; python_version < "3.13"
pillow>=11.0.0; python_version >= "3.13"
boto3==1.34.0
requests==2.31.0