# requirements.txt is now only used locally (pip install -r requirements.txt)
# Docker installs everything directly in the Dockerfile for layer caching control.
numpy<2
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
python-dotenv==1.0.1
pillow==10.3.0
boto3==1.34.0
requests==2.31.0